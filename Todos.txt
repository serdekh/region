- Daily tasks:

- (Done) -
    -
        1.1 Implement malloc failure tests for the `__region_alloc` function
        1.2: Implement testing for the new function `__region_shrink_capacity`
    2: Embed the size of a new item inside the stack region as it was initially designed
        2.1.1: Implement tests for the `__stack_region_alloc` function
    -
    -
    5: Make the tester be able to read all shared objects within a directory instead of hardcoding 
      the names inside the main file.

1: Immitate a malloc failure in test functions
   1.2 Implement malloc failure tests for the `__region_push` function
   1.3 Implement malloc failure tests for the `stack_*` functions
2.1: Implement testing for the stack functions
    2.1.2: Implement tests for the `__stack_region_push` function
    2.1.3: Implement tests for the `__stack_region_peek` function
    2.1.4: Implement tests for the `__stack_region_pop` function
3: Add other functions and tests for them such as:
    3.1 `region_collect` to get an array where each index corresponds to a node within a region
    3.2 `region_merge` to merge all the existing nodes into one with capacity combined
    3.3 `region_clone` to clone (heap) a region along with its nodes 
    3.4 `region_to_array` to allocate a dynamic array and copy the data from the region
    3.5 `stack_region_peek_at` to get a pointer to a frame at a specific index
    3.6 `stack_region_peek_at_over` to get a pointer to a frame before the last one
    3.7 `stack_region_swap` to swap the last two frames on top of the stack
    3.8 `stack_region_pop_for` to pop an `n` number of frames
    3.9 `stack_region_rotate` to shift the last three elements on top of the stack in a specified direction
4: Implement a system that maps small opcodes into complete error messages by
   grouping related errors and marking them using bits inside a word

- Big tasks (Should be Implemented as soon as all the daily tasks are complete)

1: Refactor makefiles to support different OSs. Primarly both Linux and Windows
2: Refactor the testing system to run the tests on different OS environments
   by using Docker containers
3: Write documentation for each function and make it interactive from a cli
   similar to how the `man` command works 
4: Integrate Docker to publish the project on DockerHub 