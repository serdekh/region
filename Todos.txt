- Daily tasks:

1: Add tests for the `stack_region_swap` function
2: Add additional tests for the updated `region_get_last_node` function
3: Add other functions and tests for them such as:
   3.1 `stack_region_pop_for` to pop an `n` number of frames
   3.2 `stack_region_rotate` to shift the last three elements on top of the stack in a specified direction
4: Clean up the testing infrastructure
   4.1: Make the logic (function usage, namings, error reports...) more consistent
   4.2: Refactor Makefile to be less hardcoded

- Big tasks (Should be Implemented as soon as all the daily tasks are complete)

1: Refactor makefiles to support different OSs. Primarly both Linux and Windows
2: Refactor the testing system to run the tests on different OS environments
   by using Github Actions
3: Write documentation for each function and make it interactive from a cli
   similar to how the `man` command works 
4: Integrate Docker to publish the project on DockerHub 