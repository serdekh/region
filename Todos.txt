- Daily tasks:

1: Add tests for the following functions:
   1.1: `region_get_size`,
   1.2: `region_get_capacity`,
   1.3: `stack_region_get_size`,
   1.4: `stack_region_get_capacity`
2: Reimplement the error code system to incorporate integer bitmasks instead of enums
   2.1: Define a 16 bit integer in which the specific bits would refer to the following
   attributes: boolean (is error | no error), error type (einval, enomem), function class
   (region_*, stack_region_*), narrowed error (malloc region type, malloc region type memeber),
   function subclass (push, pop, free, alloc, ...) 
3: Add documentation and a separate system that parses and prints it to the console

- Big tasks (Should be Implemented as soon as all the daily tasks are complete)

1: Refactor the testing system to run the tests on different OS environments
   by using Github Actions
2: Write documentation for each function and make it interactive from a cli
   similar to how the `man` command works 
3: Integrate Docker to publish the project on DockerHub 

- Final tasks (Not essential, but would be a great contribution to the project)

1: Add an ability to set randomized expected values 
   instead of hardcoding them during the target testing
2: Add bindings to different languages
   1.1 Python
   1.2 C++
   1.3 C# 
3: Add a documentation parser target for the web by reading the docs from
   the source code and generating a corresponding html page using a 
   certain JS framework
