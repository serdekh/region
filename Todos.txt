- Daily tasks:

1: Implement testing for the stack functions
   1.1: Implement tests for the `__stack_region_pop` function
2: Add other functions and tests for them such as:
   2.1 `region_collect` to get an array where each index corresponds to a node within a region
   2.2 `region_merge` to merge all the existing nodes into one with capacity combined
   2.3 `region_clone` to clone (heap) a region along with its nodes 
   2.4 `region_to_array` to allocate a dynamic array and copy the data from the region
   2.5 `stack_region_peek_at` to get a pointer to a frame at a specific index
   2.6 `stack_region_peek_at_over` to get a pointer to a frame before the last one
   2.7 `stack_region_swap` to swap the last two frames on top of the stack
   2.8 `stack_region_pop_for` to pop an `n` number of frames
   2.9 `stack_region_rotate` to shift the last three elements on top of the stack in a specified direction

- Big tasks (Should be Implemented as soon as all the daily tasks are complete)

1: Refactor makefiles to support different OSs. Primarly both Linux and Windows
2: Refactor the testing system to run the tests on different OS environments
   by using Docker containers
3: Write documentation for each function and make it interactive from a cli
   similar to how the `man` command works 
4: Integrate Docker to publish the project on DockerHub 