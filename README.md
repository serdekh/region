# Warning

This project has been postponed since it requires a major architecture refactoring. The initial region data structure implementation is not sufficient enough and the test system needs to be rewritten to be more consistent and more effient in terms of memory usage.

It is planned to rewrite the core of the region.h file and subsequently the test system. The original master branch will become deprecated at that point. If you want to use this implementation, be cautious since it is a subject to change.
